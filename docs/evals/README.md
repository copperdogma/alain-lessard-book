# Evals

This repo keeps a lightweight eval surface for repeatable archival operations:
scan cleanup, OCR, PDF assembly, and future website builds.

Each eval attempt should record:

- source inputs
- command run
- generated artifacts
- visual or text evidence checked
- known limitations

The registry source of truth is `docs/evals/registry.yaml`.
