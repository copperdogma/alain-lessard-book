# Alain Lessard Audio Companion

This folder contains Onward-style narration scripts generated from the cleaned OCR text. The scripts include narrative sections and intentionally exclude genealogy tables, dense lists, and the printed index.

When ElevenLabs or another narration workflow produces MP3 files, add them to a local audio folder and update `manifest.json` with public audio paths before rebuilding the site.
